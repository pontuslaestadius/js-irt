
/// #Examples
/// ```
/// assert_eq!(add(1, 1), 2);
/// assert_eq!(add(99, 1), 100);
/// assert_eq!(add(50, 50), 99);
/// ```
function add(a, b) {
    return a + b;
    let c = b + a; // this wont work.
}

function newFunction() {
    
}
