
/// #Examples
/// ```
/// let a = 99;
/// assert_eq!(add(1, 1), 2);
/// assert_eq!(add(a, 1), 100);
/// assert_eq!(add(50, a), 149);
/// ```
function add(a, b) {
    return a + b;
    let c = b + a; // this wont work.
}

function newFunction() {
    
}
