
/// #Examples
/// ```
/// let a = 99;
/// assert_eq!(mul(1, 1), 1);
/// assert_eq!(mul(500, 2), 1000);
/// assert_eq!(mul(null, 2), 0);
/// let res_a = mul(50, 3);
/// assert_eq!(res_a, 150);
/// ```
function mul(a, b) {
    return a * b;
}

